import { Point, Shape } from '../types';
import { dist2, pointToSegDist2, pointToCubicDist2 } from './shapeHelpers';

const hitTest = (
    imgPt: Point,
    tol: number,
    shapes: Shape[]
) => {

};

export default hitTest;
